export type BodyWeightTypes = {
  totalLbs: number;
  totalStone: number;
  stone: number;
  lbs: number;
  kilograms: number;
};

export type validPaths = "bodyweight-metric" | "bodyweight-stone" | "bodyweight-lbs";
